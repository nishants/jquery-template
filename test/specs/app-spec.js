(function () {
  "use strict"

  QUnit.module("MyApp", {
    setup: function () {}
  });

  QUnit.test("Should do something", function (assert) {
    assert.ok(true, "should do this.");
  });

}).call(this);